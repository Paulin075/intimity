import React from 'react';
import { useAuth } from '../hooks/use-auth';

export default function AuthStateHandler() {
  // Ce composant ne fait plus rien de navigation
  // Il peut écouter l'état utilisateur si besoin, mais ne fait rien ici
  return null;
} 